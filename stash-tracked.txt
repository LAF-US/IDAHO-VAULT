diff --git a/!/OPENROUTER-CROSS-PLATFORM.md b/!/OPENROUTER-CROSS-PLATFORM.md
index f56a7041e..0bfcdafee 100644
--- a/!/OPENROUTER-CROSS-PLATFORM.md
+++ b/!/OPENROUTER-CROSS-PLATFORM.md
@@ -37,6 +37,9 @@ python3 scripts/validate_services.py --write-matrix
 - `scripts/validate_openrouter.py` verifies the provider contract.
 - `scripts/validate_services.py` writes the canonical compatibility snapshot to
   `!/INTEGRATIONS/COMPATIBILITY.md`.
+- The runtime and launch wrappers normalize `OPENROUTER_API_KEY` /
+  `OPENAI_API_KEY` and `ANTHROPIC_AUTH_TOKEN` / `ANTHROPIC_API_KEY` so one
+  env file can survive different OS-machine conventions.
 
 ## Config (works both platforms)
 ```json
diff --git a/!/launch-claude-openrouter.cmd b/!/launch-claude-openrouter.cmd
index e456c5850..090c68aa9 100644
--- a/!/launch-claude-openrouter.cmd
+++ b/!/launch-claude-openrouter.cmd
@@ -2,16 +2,7 @@
 setlocal
 
 set "REPO_ROOT=%~dp0.."
-set "ENV_FILE=%REPO_ROOT%\.op\openrouter.env"
+set "SCRIPT=%REPO_ROOT%\scripts\Use-OpenRouterEnv.ps1"
 
-if not exist "%ENV_FILE%" (
-    echo Error: %ENV_FILE% not found
-    exit /b 1
-)
-
-for /f "usebackq tokens=1,* delims==" %%a in ("%ENV_FILE%") do (
-    set "%%a=%%b"
-)
-
-claude %*
-exit /b %ERRORLEVEL%
\ No newline at end of file
+powershell -NoProfile -ExecutionPolicy Bypass -File "%SCRIPT%" -Agent claude -CliName claude %*
+exit /b %ERRORLEVEL%
diff --git a/!/launch-codex-openrouter.cmd b/!/launch-codex-openrouter.cmd
index 329ab73e7..c0ed3182f 100644
--- a/!/launch-codex-openrouter.cmd
+++ b/!/launch-codex-openrouter.cmd
@@ -2,16 +2,7 @@
 setlocal
 
 set "REPO_ROOT=%~dp0.."
-set "ENV_FILE=%REPO_ROOT%\.op\openrouter.env"
+set "SCRIPT=%REPO_ROOT%\scripts\Use-OpenRouterEnv.ps1"
 
-if not exist "%ENV_FILE%" (
-    echo Error: %ENV_FILE% not found
-    exit /b 1
-)
-
-for /f "usebackq tokens=1,* delims==" %%a in ("%ENV_FILE%") do (
-    set "%%a=%%b"
-)
-
-codex %*
-exit /b %ERRORLEVEL%
\ No newline at end of file
+powershell -NoProfile -ExecutionPolicy Bypass -File "%SCRIPT%" -Agent codex -CliName codex %*
+exit /b %ERRORLEVEL%
diff --git a/.gitignore b/.gitignore
index 7476023dc..119a945f3 100644
--- a/.gitignore
+++ b/.gitignore
@@ -7,22 +7,34 @@ __pycache__/
 *.egg-info/
 dist/
 build/
-.venv/
-venv/
-.uv-cache/
-.pip-cache/
-.npm-cache/
-.tmp/
-.cache/
-.state/
-.pycache/
-.agent-home/
-.codex/home/
-!.python/
+.venv/*
+!.venv/
+!.venv/.gitkeep
+venv/
+.uv-cache/
+.pip-cache/
+.npm-cache/
+.tmp/*
+!.tmp/
+!.tmp/.gitkeep
+.cache/*
+!.cache/
+!.cache/.gitkeep
+.state/*
+!.state/
+!.state/.gitkeep
+.pycache/
+.agent-home/*
+!.agent-home/
+!.agent-home/.gitkeep
+.codex/home/
+!.python/
 
 # CrewAI
 .crewai_cache/
-.crewai/logs/
+.crewai/logs/*
+!.crewai/logs/
+!.crewai/logs/.gitkeep
 
 # IDE
 .vscode/*
@@ -118,8 +130,11 @@ desktop.ini
 *.tiff
 
 # Project-specific
-.env
-*.vault.json
+.env
+.env.*
+!.env.example
+!.env.template
+*.vault.json
 .remember/
 _private/
 _private/**
@@ -218,8 +233,9 @@ tempfolder.txt
 **/.claude/worktrees/
 
 # 1Password local runtime files and generated reports
-.op/openrouter.env
-.op/reports/
+.op/*.env
+!.op/*.env.template
+.op/reports/
 
 # MCP authentication state (contains OAuth tokens - NEVER track)
 .mcp-auth/
diff --git a/.obsidian/community-plugins.json b/.obsidian/community-plugins.json
index b483a09d0..327ab14b5 100644
--- a/.obsidian/community-plugins.json
+++ b/.obsidian/community-plugins.json
@@ -10,7 +10,6 @@
   "nldates-obsidian",
   "obsidian-local-rest-api",
   "mcp-tools",
-  "home-tab",
   "letterboxd-rss-sync",
   "smart-connections",
   "smart-connections-visualizer",
@@ -21,7 +20,6 @@
   "wikipedia-search",
   "msg-handler",
   "obsidian42-strange-new-worlds",
-  "obsidian-day-planner",
   "dataview",
   "obsidian-tasks-plugin",
   "calendar",
@@ -42,5 +40,8 @@
   "desk",
   "obsidian42-brat",
   "days-since",
-  "map-of-content"
+  "map-of-content",
+  "obsidian-day-planner",
+  "home-tab",
+  "obsidian-file-cooker"
 ]
\ No newline at end of file
diff --git a/.obsidian/core-plugins.json b/.obsidian/core-plugins.json
index 62768dc79..d9bdcf8ed 100644
--- a/.obsidian/core-plugins.json
+++ b/.obsidian/core-plugins.json
@@ -7,6 +7,7 @@
   "canvas": true,
   "outgoing-link": true,
   "tag-pane": true,
+  "footnotes": true,
   "properties": true,
   "page-preview": true,
   "daily-notes": true,
@@ -27,7 +28,6 @@
   "file-recovery": true,
   "publish": true,
   "sync": true,
-  "webviewer": true,
-  "footnotes": true,
-  "bases": true
+  "bases": true,
+  "webviewer": true
 }
\ No newline at end of file
diff --git a/.obsidian/plugins/obsidian-git/manifest.json b/.obsidian/plugins/obsidian-git/manifest.json
index 9e777dd4d..ac443e028 100644
--- a/.obsidian/plugins/obsidian-git/manifest.json
+++ b/.obsidian/plugins/obsidian-git/manifest.json
@@ -1,10 +1,10 @@
 {
-  "author": "Vinzent",
-  "authorUrl": "https://github.com/Vinzent03",
-  "id": "obsidian-git",
-  "name": "Git",
-  "description": "Integrate Git version control with automatic backup and other advanced features.",
-  "isDesktopOnly": false,
-  "fundingUrl": "https://ko-fi.com/vinzent",
-  "version": "2.38.0"
-}
\ No newline at end of file
+    "author": "Vinzent",
+    "authorUrl": "https://github.com/Vinzent03",
+    "id": "obsidian-git",
+    "name": "Git",
+    "description": "Integrate Git version control with automatic backup and other advanced features.",
+    "isDesktopOnly": false,
+    "fundingUrl": "https://ko-fi.com/vinzent",
+    "version": "2.38.2"
+}
diff --git a/2026-03-19.md b/2026-03-19.md
index e91d8c8b2..0d19dc591 100644
--- a/2026-03-19.md
+++ b/2026-03-19.md
@@ -25,7 +25,7 @@ PERSONAL - *LOGAN's notes to future self.*
 WATCH: add to list ; "Train Dreams" movie on Netflix 
 
 SOUNDTRACK: AJR good pretender
-- [ ] Mixtape for IDRE March 20
+- [x] Mixtape for IDRE March 20  ⏳ 2026-03-19 ✅ 2026-04-28
 - [x] Spotify tier - Duo with Aurora?
 
 DOGS: JOE > PENNY > LEXI > TROUBLE
@@ -51,7 +51,7 @@ WORK - *IDRE5411 PRODUCTION DAY*
 	- [x] Supers and graphics Check-in
 	- [x] YouTube/Soundcloud Publish
 - [ ] budget TRACKER update
-- [ ] NEWSLETTER for Friday 
+- [x] NEWSLETTER for Friday  ⏳ 2026-03-19 ✅ 2026-04-28
 
 ---
 
diff --git a/2026-04-28.md b/2026-04-28.md
index 1556c505c..13f67d6df 100644
--- a/2026-04-28.md
+++ b/2026-04-28.md
@@ -77,4 +77,4 @@ Hello Logan; unfortunately, we haven't received any forms, through your portal o
 Let us know if you need further assistance!
 Thank you,
 
-Trace, LWP Admin phonetonote
\ No newline at end of file
+Trace, LWP Admin #phonetonote
\ No newline at end of file
diff --git a/ENV.md b/ENV.md
index e69de29bb..2178cac09 100644
--- a/ENV.md
+++ b/ENV.md
@@ -0,0 +1,30 @@
+# Runtime Environment Scaffold
+
+This repo tracks runtime shape, not local runtime contents.
+
+Tracked scaffold:
+
+- `.env.example` documents safe local defaults.
+- `.venv/.gitkeep` preserves the repo-local Python environment directory.
+- `.cache/.gitkeep`, `.state/.gitkeep`, `.tmp/.gitkeep`, and `.agent-home/.gitkeep` preserve vault-local runtime containment directories.
+- `.crewai/logs/.gitkeep` preserves the CrewAI log directory.
+- `.op/openrouter.env.template` documents the OpenRouter/1Password env-file shape.
+
+Local-only files:
+
+- `.env`
+- `.env.*` except tracked examples/templates
+- `.op/*.env` except tracked examples/templates
+- all installed packages and generated files inside `.venv/`
+- all generated cache, state, temp, log, and agent-home contents
+
+Setup pattern:
+
+```bash
+cp .env.example .env
+cp .op/openrouter.env.template .op/openrouter.env
+uv venv .venv
+uv sync
+```
+
+Keep real API keys out of tracked files. Prefer `op://...` 1Password references in `.op/openrouter.env`.
diff --git a/IMPL-MESH-OPENROUTER-2026-04-24.md b/IMPL-MESH-OPENROUTER-2026-04-24.md
index 80772466a..5b197a698 100644
--- a/IMPL-MESH-OPENROUTER-2026-04-24.md
+++ b/IMPL-MESH-OPENROUTER-2026-04-24.md
@@ -229,7 +229,7 @@ The original implementation report captured the April 24 migration state. The
 active workstation now uses:
 
 - `gateway.bind = loopback`
-- `agents.defaults.model.primary = openrouter/openai/gpt-4o-mini`
+- `agents.defaults.model.primary = ollama/mistral-large`
 - OpenRouter provider models aligned to the live JSON in `~/.openclaw/openclaw.json`
 - Validator scripts now provide the live runtime contract check instead of
   relying on this report as the only source of truth
diff --git a/OPENROUTER-MESH-2026-04-24.md b/OPENROUTER-MESH-2026-04-24.md
index bc31372cf..04943f1f0 100644
--- a/OPENROUTER-MESH-2026-04-24.md
+++ b/OPENROUTER-MESH-2026-04-24.md
@@ -21,17 +21,17 @@ related:
 
 # OpenRouter Unified Mesh
 
-All AI model calls centralized through OpenRouter for:
+All AI model calls are coordinated through the mesh, with Ollama primary and OpenRouter fallback, for:
 - Single API key (1Password: `op://Vault/OpenRouter API Key/credential`)
 - Cost control via per-key rate limits
-- Free tier models as default (zero-cost routing)
+- OpenRouter fallback models for when local targets are unavailable
 - Model flexibility without rewiring
 
 ## Current Status
 
 | Component | Status | Model |
 |-----------|--------|-------|
-| OpenClaw Gateway | ✅ Running | `openrouter/openai/gpt-4o-mini` |
+| OpenClaw Gateway | ✅ Running | `ollama/mistral-large` |
 | Codex | ✅ Ready | Via launch scripts |
 | Claude Code | ✅ Ready | Via launch scripts |
 | Discord | ⚠️ Enabled | Needs token verification |
@@ -55,20 +55,21 @@ All AI model calls centralized through OpenRouter for:
               └─────────┘ └──────┘ └───────┘ └──────┘ └────────┘
 ```
 
-## Free Tier Models (Active)
+## Active Router Models
 
 | Model | Context | Use |
 |-------|---------|-----|
-| `openai/gpt-4o-mini` | 286K | Primary / default |
-| `anthropic/claude-3.5-haiku` | 131K | Claude tasks |
-| `mistralai/mistral-large-2411` | 131K | Paid / larger context |
-| `mistralai/mistral-small-2603` | 131K | Free / lightweight |
+| `openai/gpt-4o-mini` | 286K | Primary OpenRouter fallback |
+| `anthropic/claude-3.5-haiku` | 131K | Claude fallback |
+| `mistralai/mistral-large-2411` | 131K | Larger-context fallback |
+| `mistralai/mistral-small-2603` | 131K | Lightweight fallback |
 
 ## Current Live Binding
 
 - Gateway bind: `loopback`
 - Auth mode: `token`
-- Primary model: `openrouter/openai/gpt-4o-mini`
+- Primary model: `ollama/mistral-large`
+- Fallback chain: `ollama/devstral` → `ollama/qwen3.5` → `ollama/phi3:mini` → `openrouter/openai/gpt-4o-mini` → `openrouter/anthropic/claude-3.5-haiku` → `openrouter/mistralai/mistral-large-2411` → `openrouter/mistralai/mistral-small-2603`
 
 ## Bootstrap
 
diff --git a/TO DO LIST.md b/TO DO LIST.md
index e69de29bb..2b279c22e 100644
--- a/TO DO LIST.md	
+++ b/TO DO LIST.md	
@@ -0,0 +1,4 @@
+---
+date created: Monday, April 27th 2026, 10:44:38 pm
+date modified: Friday, May 1st 2026, 3:08:16 pm
+---
diff --git a/scripts/Use-OpenRouterEnv.ps1 b/scripts/Use-OpenRouterEnv.ps1
index 41710ab5f..d5d7eb277 100644
--- a/scripts/Use-OpenRouterEnv.ps1
+++ b/scripts/Use-OpenRouterEnv.ps1
@@ -25,6 +25,7 @@ $envFile = Get-EnvFilePath $vaultRoot
 $resolver = Get-ResolverScript $vaultRoot
 $runtimeHelper = Join-Path $scriptDir "Use-VaultAgentEnv.ps1"
 
+$envRequiresResolution = $false
 $requiredKeys = switch ($Agent) {
     "codex" { @("OPENAI_API_KEY", "OPENAI_BASE_URL") }
     "claude" { @("ANTHROPIC_AUTH_TOKEN", "ANTHROPIC_BASE_URL", "ANTHROPIC_API_KEY") }
@@ -33,6 +34,18 @@ $requiredKeys = switch ($Agent) {
 $needsRefresh = -not (Test-Path -LiteralPath $envFile)
 if (-not $needsRefresh) {
     $envVars = Load-EnvFile $envFile
+    if (-not $envVars.ContainsKey("OPENROUTER_API_KEY") -and $envVars.ContainsKey("OPENAI_API_KEY")) {
+        $envVars["OPENROUTER_API_KEY"] = $envVars["OPENAI_API_KEY"]
+    }
+    if (-not $envVars.ContainsKey("OPENAI_API_KEY") -and $envVars.ContainsKey("OPENROUTER_API_KEY")) {
+        $envVars["OPENAI_API_KEY"] = $envVars["OPENROUTER_API_KEY"]
+    }
+    if (-not $envVars.ContainsKey("ANTHROPIC_AUTH_TOKEN") -and $envVars.ContainsKey("ANTHROPIC_API_KEY")) {
+        $envVars["ANTHROPIC_AUTH_TOKEN"] = $envVars["ANTHROPIC_API_KEY"]
+    }
+    if (-not $envVars.ContainsKey("ANTHROPIC_API_KEY") -and $envVars.ContainsKey("ANTHROPIC_AUTH_TOKEN")) {
+        $envVars["ANTHROPIC_API_KEY"] = $envVars["ANTHROPIC_AUTH_TOKEN"]
+    }
     foreach ($requiredKey in $requiredKeys) {
         if (-not $envVars.ContainsKey($requiredKey) -or [string]::IsNullOrWhiteSpace($envVars[$requiredKey])) {
             $needsRefresh = $true
@@ -71,6 +84,31 @@ if (-not (Test-Path -LiteralPath $envFile)) {
 }
 
 $envVars = Load-EnvFile $envFile
+if (-not $envVars.ContainsKey("OPENROUTER_API_KEY") -and $envVars.ContainsKey("OPENAI_API_KEY")) {
+    $envVars["OPENROUTER_API_KEY"] = $envVars["OPENAI_API_KEY"]
+}
+if (-not $envVars.ContainsKey("OPENAI_API_KEY") -and $envVars.ContainsKey("OPENROUTER_API_KEY")) {
+    $envVars["OPENAI_API_KEY"] = $envVars["OPENROUTER_API_KEY"]
+}
+if (-not $envVars.ContainsKey("ANTHROPIC_AUTH_TOKEN") -and $envVars.ContainsKey("ANTHROPIC_API_KEY")) {
+    $envVars["ANTHROPIC_AUTH_TOKEN"] = $envVars["ANTHROPIC_API_KEY"]
+}
+if (-not $envVars.ContainsKey("ANTHROPIC_API_KEY") -and $envVars.ContainsKey("ANTHROPIC_AUTH_TOKEN")) {
+    $envVars["ANTHROPIC_API_KEY"] = $envVars["ANTHROPIC_AUTH_TOKEN"]
+}
+foreach ($requiredKey in $requiredKeys) {
+    if (-not $envVars.ContainsKey($requiredKey) -or [string]::IsNullOrWhiteSpace($envVars[$requiredKey])) {
+        Write-Error "Env file fallback cannot continue: missing required key '$requiredKey' in $envFile."
+    }
+    if ($envVars[$requiredKey] -like "op://*") {
+        $envRequiresResolution = $true
+    }
+}
+
+if ($envRequiresResolution) {
+    Write-Error "Env file fallback cannot resolve 1Password references without 'op'. Sign in to 1Password or materialize concrete env values first."
+}
+
 foreach ($key in $envVars.Keys) {
     Set-Item -Path "env:$key" -Value $envVars[$key] -ErrorAction SilentlyContinue
 }
diff --git a/scripts/openrouter_runtime.py b/scripts/openrouter_runtime.py
index 47f605b92..10f388f29 100644
--- a/scripts/openrouter_runtime.py
+++ b/scripts/openrouter_runtime.py
@@ -80,6 +80,39 @@ def parse_env_content(content: str) -> dict[str, str]:
     return values
 
 
+def normalize_env_aliases(values: dict[str, str]) -> dict[str, str]:
+    normalized = values.copy()
+
+    if not normalized.get("OPENROUTER_API_KEY") and normalized.get("OPENAI_API_KEY"):
+        normalized["OPENROUTER_API_KEY"] = normalized["OPENAI_API_KEY"]
+    if not normalized.get("OPENAI_API_KEY") and normalized.get("OPENROUTER_API_KEY"):
+        normalized["OPENAI_API_KEY"] = normalized["OPENROUTER_API_KEY"]
+
+    if not normalized.get("ANTHROPIC_AUTH_TOKEN") and normalized.get("ANTHROPIC_API_KEY"):
+        normalized["ANTHROPIC_AUTH_TOKEN"] = normalized["ANTHROPIC_API_KEY"]
+    if not normalized.get("ANTHROPIC_API_KEY") and normalized.get("ANTHROPIC_AUTH_TOKEN"):
+        normalized["ANTHROPIC_API_KEY"] = normalized["ANTHROPIC_AUTH_TOKEN"]
+
+    if not normalized.get("OPENAI_BASE_URL"):
+        normalized["OPENAI_BASE_URL"] = "https://openrouter.ai/api/v1"
+    if not normalized.get("OPENAI_MODEL"):
+        normalized["OPENAI_MODEL"] = "openrouter/auto"
+    if not normalized.get("ANTHROPIC_BASE_URL"):
+        normalized["ANTHROPIC_BASE_URL"] = "https://openrouter.ai/api"
+
+    return normalized
+
+
+def load_env_file_values(path: Path) -> dict[str, str]:
+    if not path.exists():
+        return {}
+    return normalize_env_aliases(parse_env_content(path.read_text(encoding="utf-8")))
+
+
+def env_contains_unresolved_refs(values: dict[str, str], keys: list[str]) -> bool:
+    return any(values.get(key, "").startswith("op://") for key in keys)
+
+
 def ensure_env_file(agent: str) -> Path:
     required_keys = {
         "codex": ["OPENAI_API_KEY", "OPENAI_BASE_URL"],
@@ -102,8 +135,10 @@ def ensure_env_file(agent: str) -> Path:
     return ENV_FILE
 
 
-def exec_agent(agent: str, cli_name: str, args: list[str]) -> int:
+def exec_agent(agent: str, cli_name: str, args: list[str], env_overrides: dict[str, str] | None = None) -> int:
     env = apply_runtime_env(agent)
+    if env_overrides:
+        env.update(env_overrides)
     resolved_cli = shutil.which(cli_name, path=env.get("PATH"))
     if resolved_cli is None:
         raise SystemExit(f"Could not find '{cli_name}' on PATH.")
@@ -143,7 +178,18 @@ def launch_agent(agent: str, cli_name: str, args: list[str]) -> int:
             result = subprocess.run(command, check=False)
             return result.returncode
 
-    return exec_agent(agent, cli_name, args)
+    env_values = load_env_file_values(env_file)
+    required_keys = {
+        "codex": ["OPENAI_API_KEY", "OPENAI_BASE_URL"],
+        "claude": ["ANTHROPIC_AUTH_TOKEN", "ANTHROPIC_BASE_URL", "ANTHROPIC_API_KEY"],
+    }[agent]
+    if env_contains_unresolved_refs(env_values, required_keys):
+        raise SystemExit(
+            "OpenRouter env-file fallback is not usable without 1Password resolution.\n"
+            f"Resolve {env_file} with `op run` or replace its `op://...` refs with concrete values."
+        )
+
+    return exec_agent(agent, cli_name, args, env_overrides=env_values or None)
 
 
 def main() -> int:
diff --git a/scripts/validate_openrouter.py b/scripts/validate_openrouter.py
index 3db58ff10..b992766fc 100644
--- a/scripts/validate_openrouter.py
+++ b/scripts/validate_openrouter.py
@@ -42,6 +42,23 @@ def parse_env_file(path: Path) -> dict[str, str]:
     return values
 
 
+def normalize_env_aliases(values: dict[str, str]) -> dict[str, str]:
+    normalized = values.copy()
+    if not normalized.get("OPENROUTER_API_KEY") and normalized.get("OPENAI_API_KEY"):
+        normalized["OPENROUTER_API_KEY"] = normalized["OPENAI_API_KEY"]
+    if not normalized.get("OPENAI_API_KEY") and normalized.get("OPENROUTER_API_KEY"):
+        normalized["OPENAI_API_KEY"] = normalized["OPENROUTER_API_KEY"]
+    if not normalized.get("ANTHROPIC_AUTH_TOKEN") and normalized.get("ANTHROPIC_API_KEY"):
+        normalized["ANTHROPIC_AUTH_TOKEN"] = normalized["ANTHROPIC_API_KEY"]
+    if not normalized.get("ANTHROPIC_API_KEY") and normalized.get("ANTHROPIC_AUTH_TOKEN"):
+        normalized["ANTHROPIC_API_KEY"] = normalized["ANTHROPIC_AUTH_TOKEN"]
+    if not normalized.get("OPENAI_BASE_URL"):
+        normalized["OPENAI_BASE_URL"] = "https://openrouter.ai/api/v1"
+    if not normalized.get("ANTHROPIC_BASE_URL"):
+        normalized["ANTHROPIC_BASE_URL"] = "https://openrouter.ai/api"
+    return normalized
+
+
 def _check_path_exists(name: str, path: Path) -> ValidationResult:
     return ValidationResult(
         name=name,
@@ -90,7 +107,7 @@ def validate_openrouter_env() -> list[ValidationResult]:
     results.append(_check_path_exists("OpenRouter env file", ENV_FILE))
     results.append(_check_op_status())
 
-    env_values = parse_env_file(ENV_FILE)
+    env_values = normalize_env_aliases(parse_env_file(ENV_FILE))
     required_keys = {
         "OPENROUTER_API_KEY": "OpenRouter API key ref/value",
         "OPENAI_API_KEY": "OpenAI compatibility key ref/value",
@@ -152,6 +169,28 @@ def validate_openrouter_env() -> list[ValidationResult]:
                     )
                 )
 
+        if any(env_values.get(key, "").startswith("op://") for key in required_keys):
+            op_available = shutil.which("op") is not None and subprocess.run(
+                ["op", "whoami"],
+                stdout=subprocess.DEVNULL,
+                stderr=subprocess.DEVNULL,
+                check=False,
+            ).returncode == 0
+            results.append(
+                ValidationResult(
+                    name="Env-file fallback",
+                    ok=op_available or all(
+                        not env_values.get(key, "").startswith("op://") for key in required_keys
+                    ),
+                    severity="warn" if not op_available else "info",
+                    detail=(
+                        "1Password resolution is available for this env file."
+                        if op_available
+                        else "env file contains unresolved `op://...` references and requires 1Password resolution."
+                    ),
+                )
+            )
+
     return results
 
 
diff --git a/skills/openrouter-config.md b/skills/openrouter-config.md
index 7cc6bb395..45db6992d 100644
--- a/skills/openrouter-config.md
+++ b/skills/openrouter-config.md
@@ -31,7 +31,7 @@ python3 scripts/validate_openrouter.py
 - Materializes `.op/openrouter.env` for Codex and Claude launchers.
 - Prefers `op://...` references instead of plaintext credentials.
 - Validates the runtime contract with `scripts/validate_openrouter.py`.
-- Keeps OpenClaw pointed at OpenRouter as the provider.
+- Keeps OpenClaw aligned to the vault's live Mistral-first provider chain.
 - Keeps model IDs in OpenRouter format, including the `mistralai/` prefix.
 
 ## Current OpenClaw contract
@@ -41,6 +41,17 @@ Use these values unless the vault explicitly updates them:
 {
   "models": {
     "providers": {
+      "ollama": {
+        "apiKey": "ollama",
+        "baseUrl": "http://127.0.0.1:11434/v1",
+        "models": [
+          {"id": "mistral-large"},
+          {"id": "devstral"},
+          {"id": "qwen3.5"},
+          {"id": "phi3:mini"},
+          {"id": "qwen2.5:3b"}
+        ]
+      },
       "openrouter": {
         "apiKey": "env:OPENROUTER_API_KEY",
         "baseUrl": "https://openrouter.ai/api/v1",
@@ -55,7 +66,18 @@ Use these values unless the vault explicitly updates them:
   },
   "agents": {
     "defaults": {
-      "model": {"primary": "openrouter/openai/gpt-4o-mini"}
+      "model": {
+        "primary": "ollama/mistral-large",
+        "fallbacks": [
+          "ollama/devstral",
+          "ollama/qwen3.5",
+          "ollama/phi3:mini",
+          "openrouter/openai/gpt-4o-mini",
+          "openrouter/anthropic/claude-3.5-haiku",
+          "openrouter/mistralai/mistral-large-2411",
+          "openrouter/mistralai/mistral-small-2603"
+        ]
+      }
     }
   }
 }
@@ -68,5 +90,8 @@ Use these values unless the vault explicitly updates them:
   pointed at 1Password.
 - Treat `openrouter/` model prefixes as the gateway-level selection path and
   `openai/`, `anthropic/`, and `mistralai/` as provider model IDs.
+- The runtime normalizes `OPENROUTER_API_KEY` / `OPENAI_API_KEY` and
+  `ANTHROPIC_AUTH_TOKEN` / `ANTHROPIC_API_KEY` aliases so the same env file
+  works across macOS and Windows.
 - Treat this file as reference guidance only. The live automation surface is
   the resolver plus the validation scripts above.
diff --git a/src/idaho_vault/sparkseed.py b/src/idaho_vault/sparkseed.py
index c99d87ac7..27aa5d400 100644
--- a/src/idaho_vault/sparkseed.py
+++ b/src/idaho_vault/sparkseed.py
@@ -7,8 +7,11 @@ import os
 import shutil
 import subprocess
 import sys
+import time
+from contextlib import suppress
 from dataclasses import dataclass
 from pathlib import Path
+from socket import create_connection
 
 
 class SparkseedError(RuntimeError):
@@ -29,7 +32,7 @@ REQUIRED_SECRET_FIELDS = (
     SecretField(item="OpenRouter API Key", field="credential", env_name="OPENROUTER_API_KEY"),
     SecretField(item="OpenClaw Gateway Token", field="credential", env_name="GATEWAY_TOKEN"),
     SecretField(item="Discord OpenClaw Bot", field="credential", env_name="DISCORD_BOT_TOKEN"),
-    SecretField(item="Discord OpenClaw Bot", field="credential", env_name="DISCORD_APP_ID"),
+    SecretField(item="Discord OpenClaw Bot", field="applicationId", env_name="DISCORD_APP_ID"),
     SecretField(item="Signal", field="number", env_name="SIGNAL_NUMBER"),
     SecretField(item="Nostr (Orpheus)", field="password", env_name="NOSTR_PRIVATE_KEY"),
     SecretField(item="Nostr (Orpheus)", field="username", env_name="NOSTR_PUBLIC_KEY"),
@@ -42,6 +45,11 @@ REQUIRED_SECRET_FIELDS = (
     SecretField(item="Telegram Bot", field="credential", env_name="TELEGRAM_BOT_TOKEN"),
 )
 
+OLLAMA_HOST = "127.0.0.1"
+OLLAMA_PORT = 11434
+OLLAMA_START_TIMEOUT_SECONDS = 30
+OLLAMA_START_POLL_INTERVAL_SECONDS = 1
+
 OPENCLAW_BOOT_SEQUENCE = (
     ("gateway", "run"),
     ("secrets", "reload"),
@@ -171,6 +179,49 @@ def _load_env_file(env_path: Path) -> dict[str, str]:
     return env
 
 
+def _ollama_is_listening() -> bool:
+    """Return True when the local Ollama server is reachable on its default port."""
+
+    with suppress(OSError):
+        with create_connection((OLLAMA_HOST, OLLAMA_PORT), timeout=0.5):
+            return True
+    return False
+
+
+def ensure_ollama_running() -> None:
+    """Start the local Ollama server if it is not already accepting connections."""
+
+    if _ollama_is_listening():
+        _announce("Local Ollama server is already listening on 127.0.0.1:11434.")
+        return
+
+    ollama_path = _require_command("ollama")
+    log_dir = Path.home() / ".openclaw" / "logs"
+    log_dir.mkdir(parents=True, exist_ok=True)
+    log_path = log_dir / "ollama.log"
+
+    _announce("Starting the local Ollama server for the Mistral-first ignition path.")
+    with log_path.open("a", encoding="utf-8") as log_file:
+        subprocess.Popen(
+            [ollama_path, "serve"],
+            env=os.environ.copy(),
+            stdout=log_file,
+            stderr=log_file,
+            start_new_session=True,
+        )
+
+    deadline = time.time() + OLLAMA_START_TIMEOUT_SECONDS
+    while time.time() < deadline:
+        if _ollama_is_listening():
+            _announce("Local Ollama server is ready on 127.0.0.1:11434.")
+            return
+        time.sleep(OLLAMA_START_POLL_INTERVAL_SECONDS)
+
+    raise SparkseedError(
+        "Timed out waiting for the local Ollama server to listen on 127.0.0.1:11434."
+    )
+
+
 def _announce(message: str) -> None:
     print(message)
 
@@ -183,27 +234,30 @@ def run_sparkseed() -> None:
     _announce("SPARKSEED bootstrap: preparing the local OpenClaw edge from vault state.")
     _announce("=" * 83)
     _announce("")
-    _announce("[1/4] Loading the required OpenClaw secrets from 1Password.")
+    _announce("[1/5] Ensuring the local Ollama server is available.")
+    ensure_ollama_running()
+    _announce("-" * 83)
+    _announce("[2/5] Loading the required OpenClaw secrets from 1Password.")
     env = os.environ.copy()
     env.update(build_secret_env())
     _announce("Resolved gateway, Discord, Signal, and OpenRouter values for this process.")
     _announce("-" * 83)
 
-    for index, command_tail in enumerate(OPENCLAW_BOOT_SEQUENCE, start=2):
+    for index, command_tail in enumerate(OPENCLAW_BOOT_SEQUENCE, start=3):
         command = [openclaw_path, *command_tail]
         if command_tail == ("gateway", "run"):
-            _announce(f"[{index}/4] Starting the gateway surface (OpenRouter routed).")
+            _announce(f"[{index}/5] Starting the gateway surface (Mistral-first, OpenRouter fallback).")
         elif command_tail == ("secrets", "reload"):
-            _announce(f"[{index}/4] Reloading the active secret manifest.")
+            _announce(f"[{index}/5] Reloading the active secret manifest.")
         else:
-            _announce(f"[{index}/4] Auditing the current secret view.")
+            _announce(f"[{index}/5] Auditing the current secret view.")
         result = _run_stream(command, env=env)
         _check_success(result, command)
-        if index < 4:
+        if index < 5:
             _announce("-" * 83)
 
     _announce("=" * 83)
-    _announce("SPARKSEED bootstrap complete. OpenRouter is now the unified model layer.")
+    _announce("SPARKSEED bootstrap complete. Ollama is live and OpenRouter remains the fallback layer.")
     _announce("=" * 83)
 
 
diff --git a/tests/test_openrouter_runtime.py b/tests/test_openrouter_runtime.py
index b737d14b7..ca24bdb80 100644
--- a/tests/test_openrouter_runtime.py
+++ b/tests/test_openrouter_runtime.py
@@ -78,6 +78,37 @@ class OpenRouterRuntimeTest(unittest.TestCase):
         ensure_op_signed_in.assert_not_called()
         ensure_env_file.assert_not_called()
 
+    def test_launch_agent_falls_back_to_plain_env_file_values_without_op(self) -> None:
+        env_values = {
+            "OPENAI_API_KEY": "plain-key",
+            "OPENAI_BASE_URL": "https://openrouter.ai/api/v1",
+        }
+        with (
+            patch.object(openrouter_runtime, "ensure_env_file", return_value=openrouter_runtime.ENV_FILE),
+            patch.object(openrouter_runtime.shutil, "which", return_value=None),
+            patch.object(openrouter_runtime, "load_env_file_values", return_value=env_values),
+            patch.object(openrouter_runtime, "exec_agent", return_value=0) as exec_agent,
+        ):
+            status = openrouter_runtime.launch_agent("codex", "codex", [])
+
+        self.assertEqual(status, 0)
+        exec_agent.assert_called_once_with("codex", "codex", [], env_overrides=env_values)
+
+    def test_launch_agent_refuses_unresolved_op_refs_without_op(self) -> None:
+        env_values = {
+            "OPENAI_API_KEY": "op://Vault/OpenRouter API Key/credential",
+            "OPENAI_BASE_URL": "https://openrouter.ai/api/v1",
+        }
+        with (
+            patch.object(openrouter_runtime, "ensure_env_file", return_value=openrouter_runtime.ENV_FILE),
+            patch.object(openrouter_runtime.shutil, "which", return_value=None),
+            patch.object(openrouter_runtime, "load_env_file_values", return_value=env_values),
+        ):
+            with self.assertRaises(SystemExit) as exc:
+                openrouter_runtime.launch_agent("codex", "codex", [])
+
+        self.assertIn("OpenRouter env-file fallback is not usable without 1Password resolution", str(exc.exception))
+
     def test_exec_agent_resolves_full_cli_path(self) -> None:
         with (
             patch.object(openrouter_runtime, "apply_runtime_env", return_value={"PATH": r"C:\mock\bin"}),
diff --git a/tests/test_sparkseed.py b/tests/test_sparkseed.py
index 7571563d7..3b0bb052a 100644
--- a/tests/test_sparkseed.py
+++ b/tests/test_sparkseed.py
@@ -18,44 +18,66 @@ class TestSparkseed(unittest.TestCase):
     def test_build_secret_env_reads_required_fields(self) -> None:
         responses = {
             ("op", "whoami"): CompletedProcess(["op", "whoami"], 0, stdout="logan\n", stderr=""),
+            (
+                "op",
+                "item",
+                "get",
+                "OpenRouter API Key",
+                "--field",
+                "credential",
+                "--vault",
+                "Vault",
+            ): CompletedProcess([], 0, stdout="openrouter-key\n", stderr=""),
             (
                 "op",
                 "item",
                 "get",
                 "OpenClaw Gateway Token",
                 "--field",
-                "token",
+                "credential",
+                "--vault",
+                "Vault",
             ): CompletedProcess([], 0, stdout="gateway-token\n", stderr=""),
             (
                 "op",
                 "item",
                 "get",
-                "OpenClaw Discord Bot",
+                "Discord OpenClaw Bot",
                 "--field",
-                "token",
+                "credential",
+                "--vault",
+                "Vault",
             ): CompletedProcess([], 0, stdout="discord-token\n", stderr=""),
             (
                 "op",
                 "item",
                 "get",
-                "OpenClaw Discord Bot",
+                "Discord OpenClaw Bot",
                 "--field",
                 "applicationId",
+                "--vault",
+                "Vault",
             ): CompletedProcess([], 0, stdout="discord-app\n", stderr=""),
             (
                 "op",
                 "item",
                 "get",
-                "OpenClaw Signal Account",
+                "Signal",
                 "--field",
                 "number",
+                "--vault",
+                "Vault",
             ): CompletedProcess([], 0, stdout="+12085551212\n", stderr=""),
         }
 
         def fake_run(command: list[str], **_: object) -> CompletedProcess[str]:
-            return responses[tuple(command)]
+            return responses.get(
+                tuple(command),
+                CompletedProcess(command, 1, stdout="", stderr="missing"),
+            )
 
         with (
+            mock.patch.dict("os.environ", {"OP_ACCOUNT": "1"}, clear=False),
             mock.patch("idaho_vault.sparkseed.shutil.which", return_value="present"),
             mock.patch("idaho_vault.sparkseed.subprocess.run", side_effect=fake_run),
         ):
@@ -64,6 +86,7 @@ class TestSparkseed(unittest.TestCase):
         self.assertEqual(
             resolved,
             {
+                "OPENROUTER_API_KEY": "openrouter-key",
                 "GATEWAY_TOKEN": "gateway-token",
                 "DISCORD_BOT_TOKEN": "discord-token",
                 "DISCORD_APP_ID": "discord-app",
@@ -71,8 +94,30 @@ class TestSparkseed(unittest.TestCase):
             },
         )
 
-    def test_run_sparkseed_runs_openclaw_sequence_with_secret_env(self) -> None:
+    def test_ensure_ollama_running_starts_server_when_missing(self) -> None:
+        proc = mock.Mock()
+        proc.poll.side_effect = [None, None, None]
+        order: list[str] = []
+
+        def fake_sleep(_: float) -> None:
+            order.append("sleep")
+
+        with (
+            mock.patch("idaho_vault.sparkseed._ollama_is_listening", side_effect=[False, False, True]),
+            mock.patch("idaho_vault.sparkseed._require_command", return_value="ollama"),
+            mock.patch("idaho_vault.sparkseed.Path.home", return_value=Path("/tmp")),
+            mock.patch("idaho_vault.sparkseed.subprocess.Popen", return_value=proc) as popen,
+            mock.patch("idaho_vault.sparkseed.time.sleep", side_effect=fake_sleep),
+            mock.patch("builtins.print"),
+        ):
+            sparkseed.ensure_ollama_running()
+
+        popen.assert_called_once()
+        self.assertIn("sleep", order)
+
+    def test_run_sparkseed_runs_ollama_before_openclaw_sequence_with_secret_env(self) -> None:
         streamed_calls: list[tuple[list[str], dict[str, str]]] = []
+        order: list[str] = []
 
         def fake_run(command: list[str], **kwargs: object) -> CompletedProcess[str]:
             env = kwargs.get("env")
@@ -81,10 +126,11 @@ class TestSparkseed(unittest.TestCase):
             return CompletedProcess(command, 0, stdout="", stderr="")
 
         with (
-            mock.patch("idaho_vault.sparkseed._require_command"),
+            mock.patch("idaho_vault.sparkseed.ensure_ollama_running", side_effect=lambda: order.append("ollama")),
+            mock.patch("idaho_vault.sparkseed._require_command", return_value="openclaw"),
             mock.patch(
                 "idaho_vault.sparkseed.build_secret_env",
-                return_value={
+                side_effect=lambda: order.append("secrets") or {
                     "GATEWAY_TOKEN": "gateway-token",
                     "DISCORD_BOT_TOKEN": "discord-token",
                     "DISCORD_APP_ID": "discord-app",
@@ -96,10 +142,11 @@ class TestSparkseed(unittest.TestCase):
         ):
             sparkseed.run_sparkseed()
 
+        self.assertEqual(order[:2], ["ollama", "secrets"])
         self.assertEqual(
             [command for command, _ in streamed_calls],
             [
-                ["openclaw", "gateway", "start"],
+                ["openclaw", "gateway", "run"],
                 ["openclaw", "secrets", "reload"],
                 ["openclaw", "secrets", "audit"],
             ],
