---
date: 2026-08-16
branch: claude/elegant-archimedes-0zqhsc
author: Claude Code (software NAME; no TITLE/OFFICE)
authority: "* — agent session record; not LOGAN. Logan holds the verdict."
status: staged
related:
  - CONSTITUTION
  - VAULT-CONVENTIONS
  - WITNESS-CI-RATE-LIMIT-STORM-AUDIT-2026-08-16
---

# Session Anchor — 2026-08-16

Scheduled routine: 48h CI failure audit for `LAF-US/IDAHO-VAULT`, per Logan's standing
task. Full findings and root-cause chain are in
`WITNESS-CI-RATE-LIMIT-STORM-AUDIT-2026-08-16.md`; this anchor is the short pointer.

## What happened

1. Found a live rate-limit storm: `Review Feedback Loop` failing continuously
   (every 2–9s) on PR #962/#950, taking Codacy and CodeQL checks down as collateral.
   Root cause: `review_feedback_loop.py::acknowledge_apply()` ran a 6-call label
   sweep (`ensure_labels()`) on *every* PR comment before checking whether the
   comment even mattered, and 15–20 third-party review bots comment on every push.
2. Fixed it: moved `ensure_labels()` to only run on the actual label-mutation path.
   Regression-tested (4 new passing tests).
3. Fixing that test required actually running the test file, which surfaced that
   **none of the vault's ten `tests-test_*.py` root files had ever successfully
   collected or run** — three stacked bugs (`conftest.py` collection hook, a
   `parents[1]` path bug in all ten files, a missing `sys.path` entry for
   `.github/scripts`). Fixed all three, mechanically, without changing test
   semantics. Four of the ten files still fail on unrelated missing-module errors
   (`idaho_vault.five_wizards`, `idaho_vault.sparkseed`, `scripts/health_monitor.py`,
   `scripts/validate_bootstrap.py`) — named as follow-up, not fixed here. No CI
   workflow invokes `pytest` at all; flagged for Logan, not decided unilaterally.
4. Notified Logan via push notification while the storm was still live (this is a
   scheduled/unattended routine — that's the actual deliverable of the run).

## Session completion

Work anchored in this vault note + the WITNESS note before compaction, per
`CONSTITUTION.md`: "Chat is ephemeral. Vault is the record." Session id:
`session_01LTD66ZEF1tduSVnbvcR6H7`.
