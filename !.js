// Stub source so CodeQL's JavaScript/TypeScript analysis has real code to
// extract, resolving the "no source code seen during build" config error.
// Not imported or executed anywhere — its only job is to be analyzable.
function codeqlStub() {
  return "noop";
}
