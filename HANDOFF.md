# HANDOFF: vault-deploy.zip

Read `!ADMINISTRATION/Claude.md` from this zip first. It is your constitution for this task.

Then:

1. Check what is already in `!ADMINISTRATION/` in the live repo
2. Deploy `!ADMINISTRATION/` files from this zip without overwriting anything already there that is not in this zip
3. Deploy `.github/scripts/` and `.github/workflows/` — these replace v1 equivalents already in the repo
4. Place `vault_push.py` at repo root

Do not commit anything until you have confirmed no collisions. Report back to Logan before pushing.
