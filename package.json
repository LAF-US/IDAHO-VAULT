{
  "scripts": {
    "format": "prettier --write package.json .github/workflows/*.yml .github/actions/**/*.yml",
    "format:check": "prettier --check package.json .github/workflows/*.yml .github/actions/**/*.yml"
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
