---
name: Agent signatures in vault files
description: Agents should sign their work in vault files — do not flag or remove agent signatures
type: feedback
---

Agents sign their work in vault files (e.g., `-G` for Gemini, `-C` for Claude). Do not treat agent signatures as non-standard or propose removing them.

**Why:** Logan explicitly corrected this — agent attribution is intentional vault convention.

**How to apply:** When reviewing agent-created files, accept signatures as valid. Do not recommend stripping them.
