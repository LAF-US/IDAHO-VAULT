# Runtime Health Log

This file is the canonical append-only destination for runtime/provider
health snapshots generated by `scripts/health_monitor.py --write-log`.

The log is operational evidence, not doctrine. Older entries remain useful as
breadcrumbs, but the newest snapshot is the live status signal.
