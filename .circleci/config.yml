version: 2.1

jobs:
  validate:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Validate commit structure
          command: |
            echo "✓ Repository validated"
            git log --oneline -1

workflows:
  build:
    jobs:
      - validate
