# hadolint — Codacy toggle. `ignored: []` suppresses nothing, and keeps the
# file a valid mapping; a comments-only file parses as null, which hadolint
# rejects. No Dockerfile here yet, so reach is zero.
#
# `trustedRegistries` is the one worth adding first, and it is an allowlist:
# setting it rejects every registry not listed.

ignored: []
