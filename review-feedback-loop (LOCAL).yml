name: Review Feedback Loop

on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, reopened, ready_for_review, synchronize]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  acknowledge-copilot-apply:
    if: >
      github.event_name == 'issue_comment' &&
      github.event.issue.pull_request
    runs-on: ubuntu-latest
    steps:
      - name: Checkout trusted workflow surfaces
<<<<<<< HEAD:.github/workflows/review-feedback-loop.yml
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
=======
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
>>>>>>> origin/logan/obsidian:review-feedback-loop.yml
        with:
          ref: ${{ github.event.repository.default_branch }}

      - name: Mark PR as waiting on Copilot follow-up
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          COMMENT_BODY: ${{ github.event.comment.body }}
          REPO_NAME: ${{ github.event.repository.name }}
        run: |
          python3 .github/scripts/review_feedback_loop.py \
            acknowledge-apply \
            --owner "${{ github.repository_owner }}" \
            --repo "$REPO_NAME" \
            --pr-number "${{ github.event.issue.number }}" \
            --comment-author "${{ github.event.comment.user.login }}" \
            --author-association "${{ github.event.comment.author_association }}" \
            --comment-body "$COMMENT_BODY"

  sweep-review-threads:
    if: github.event_name == 'pull_request_target'
    # One run per PR, newest wins -- same reasoning as sync-review-state in
    # review-response.yml. `synchronize` fires on every push, so a branch pushed
    # three times in a minute paid for three full `_fetch_pr` graph fetches, of
    # which only the last described the branch as it actually stood.
    #
    # Cancelling is safe: `_resolve_outdated_resolvable_threads` walks whatever
    # is unresolved-and-outdated right now and skips anything already handled,
    # so a partially-completed sweep is not a corrupt one -- the next run picks
    # up the remainder. Arming auto-merge is likewise a function of current
    # state, not of the event that triggered it.
    # `github.event_name` is in the key deliberately: this workflow also fires on
    # `issue_comment`, where `pull_request.number` is empty and `issue.number`
    # would be the SAME number for a PR comment. Without the namespace, a comment
    # event could contend with -- and cancel -- a live sweep for that PR.
    concurrency:
      group: sweep-review-threads-${{ github.event_name }}-${{ github.event.pull_request.number }}
      cancel-in-progress: true
    runs-on: ubuntu-latest
    permissions:
      # contents: write is required to enable merge-queue auto-merge
      # (enablePullRequestAutoMerge is gated like resolveReviewThread, #546). sync-pr
      # arms eligible, non-protected-path PRs (#521/#527 reversal, 2026-06-17). Scoped
      # scoped to this job only; outside this job, `contents` stays read-only at the
      # workflow level (the other jobs do not get write access).
      contents: write
<<<<<<< HEAD:.github/workflows/review-feedback-loop.yml
      pull-requests: write
    steps:
      - name: Checkout trusted workflow surfaces
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
=======
      issues: write
      pull-requests: write
    steps:
      - name: Checkout trusted workflow surfaces
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
>>>>>>> origin/logan/obsidian:review-feedback-loop.yml
        with:
          ref: ${{ github.event.repository.default_branch }}

      - name: Sweep outdated advisory threads after PR updates
        # The arm actor becomes the future enqueue actor, and GITHUB_TOKEN-actored
        # events never dispatch workflow runs (GitHub anti-recursion) — prefer the
        # user-scoped MERGE_QUEUE_TOKEN so armed PRs enqueue with a dispatching actor.
        env:
<<<<<<< HEAD:.github/workflows/review-feedback-loop.yml
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
=======
          GH_TOKEN: ${{ secrets.MERGE_QUEUE_TOKEN || secrets.GITHUB_TOKEN }}
>>>>>>> origin/logan/obsidian:review-feedback-loop.yml
          REPO_NAME: ${{ github.event.repository.name }}
        run: |
          python3 .github/scripts/review_feedback_loop.py \
            sync-pr \
            --owner "${{ github.repository_owner }}" \
            --repo "$REPO_NAME" \
            --pr-number "${{ github.event.pull_request.number }}" \
            --sync-actor "${{ github.actor }}"

  verify-agent-claims:
    # IF 7 (brass-mouth reliability is per-utterance, not per-agent) per
    # !/ARBORSCAPE-PR-EXPANSION-2026-05-22.md (Phase 1a). Fires broadly on PR
    # comments; the subcommand filters narrowly via regex and only posts a
    # divergence note when the agent's claim disagrees with the institutional
    # GitHub state. The marker on its own divergence comment prevents recursion
    # and duplicate noise.
    if: >
      github.event_name == 'issue_comment' &&
      github.event.issue.pull_request
    runs-on: ubuntu-latest
    steps:
      - name: Checkout trusted workflow surfaces
<<<<<<< HEAD:.github/workflows/review-feedback-loop.yml
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
=======
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
>>>>>>> origin/logan/obsidian:review-feedback-loop.yml
        with:
          ref: ${{ github.event.repository.default_branch }}

      - name: Verify agent completion claims against PR state
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          COMMENT_BODY: ${{ github.event.comment.body }}
          REPO_NAME: ${{ github.event.repository.name }}
        run: |
          python3 .github/scripts/review_feedback_loop.py \
            verify-claim \
            --owner "${{ github.repository_owner }}" \
            --repo "$REPO_NAME" \
            --pr-number "${{ github.event.issue.number }}" \
            --comment-author "${{ github.event.comment.user.login }}" \
            --comment-body "$COMMENT_BODY"
