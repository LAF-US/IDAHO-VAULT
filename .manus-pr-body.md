## Summary

This review branch establishes the D&D SRD 5.2.1 as both a durable local reference and a traceable upstream reference, then aligns dependency-update documentation and proposal automation with the current uv-centered workflow.

| Area | Included change |
|---|---|
| SRD reference | Adds `DND-SRD-5.2.1/` as a CC BY 4.0 vendored Markdown snapshot with its provenance record. |
| Upstream traceability | Adds `DND-SRD-5.2.1-UPSTREAM/` as a submodule pinned to `1b4b99dcb786cdd1a2fb26f8acec1551191f1ca4`. |
| Dependency contract | Documents `pyproject.toml` as the declaration surface, `uv.lock` as the canonical resolved graph, and `requirements.txt` as a generated compatibility export. |
| Dependabot | Configures review-only GitHub Actions and Git submodule proposals for every other Thursday at noon, `America/Denver`. |

## Validation

The Dependabot YAML structure, cron schedules, documentation consistency, submodule declaration, and `uv.lock` consistency were validated. The lock check resolved 149 packages using a compatible Python 3.12 interpreter.

## Review posture

No update PR is auto-merged. The vendored SRD snapshot remains independent of the upstream submodule; a submodule proposal advances only the upstream gitlink and does not overwrite the snapshot or its provenance.
