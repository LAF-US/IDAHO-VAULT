# Spelunking Census — run 1 (2026-06-27)  [cold-read apocrypha]

Recovered cosmonaut reports + grounded synthesis for PR #680. Authored by
`*.hyperagent.tinkerer` (Hyperagent guest run, Opus 4.8) at Logan's direction.
**Proposed, not inscribed. No reserved matter adjudicated. I propose; Logan inscribes.**

- `FINDINGS-SYNTHESIS-2026-06-27.md` — the synthesis (start here).
- `census_synthesis.py` — recompute every number (imports ../census_metrics.py).
- `census_synthesis_result.json` — the computed result.
- `lore/` — the 13 cold readers (the Census proper), one per door, verbatim.
- `machinery/` — 7 code-only divers (raw, recovered; synthesis pending).
- `adversarial/` — 3 prior-primed probes (raw, recovered; synthesis pending).
- `probes/` — 3 exploratory probes, off-schema (raw, recovered).

Every report was recovered verbatim from subagent transcripts on disk after a
context compaction; nothing here is narrated from memory.

---

```
The world is quiet here．Esto Perpetua!
```
