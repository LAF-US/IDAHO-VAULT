# The *default* assumption for the VAULT is that things need to be tracked. The *only* exceptions to that sole assumption are **secrets** and **exhaust**. Tread lightly here.

!**.md

# Ollama key
.ollama/id_*

# Honcho AI config (contain API key)
honcho.json

.npm*/

# SSH (this needs refined!)
.ssh/*

.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db
._*

# Microsoft Office temporary lock files
~$*.doc
~$*.docx
~$*.xls
~$*.xlsx
~$*.ppt
~$*.pptx

# Added by ggshield
.cache_ggshield

# Jupyter autosaves
.ipynb_checkpoints/

# Android Keys
.android/adbkey
.android/adbkey.pub
**/adbkey
**/adbkey.pub
**/hostkeys

# ESTO PERPETUA!

# --- secret-bearing agent-runtime state (added 2026-08-13 after secret-scanning incident) ---
# Agent homes carry .env files, auth tokens, and session logs that record API calls.
# These classes never belong in a public repo; gitleaks pre-commit gate enforces the rest.
.env
.env.*
*.env
**/.env.bak*
.hermes/
.vibe/
**/state-snapshots/
**/archived_sessions/
rollout-20*.jsonl
session-20*.jsonl
.claude.json
.claude.json.*
.claude.json*backup*
.claude/backups/
