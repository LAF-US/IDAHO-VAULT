name: Project Courier - Ledger Backup

on:
  push:
    branches: [ main ]
    paths:
      - '!/**'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry Run'
        required: true
        default: true
        type: boolean

jobs:
  backup-ledger:
    name: Backup !/ Ledger to GCS
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    env:
      OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Fail clearly when 1Password token is missing
        if: env.OP_SERVICE_ACCOUNT_TOKEN == ''
        run: |
          echo "OP_SERVICE_ACCOUNT_TOKEN is not configured; vault-courier cannot load GCP_SA_KEY from 1Password." >&2
          exit 1

      - name: Load Secrets from 1Password
        if: env.OP_SERVICE_ACCOUNT_TOKEN != ''
        uses: 1password/load-secrets-action@v4
        with:
          export-env: true
        env:
          GCP_SA_KEY: op://vault-operations/idaho-vault-courier-key/credential

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ env.GCP_SA_KEY }}


      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v3

      - name: Execute Courier Sync
        run: |
          chmod +x .github/scripts/vault-courier-sync.sh
          if [[ "${{ github.event_name }}" == "workflow_dispatch" && "${{ inputs.dry_run }}" == "true" ]]; then
            ./.github/scripts/vault-courier-sync.sh --dry-run
          else
            ./.github/scripts/vault-courier-sync.sh
          fi
