name: smoke

on:
  merge_group:
  push:
  pull_request:
    branches: [main]

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Run Smoke Tests
        run: echo "Running smoke check..."
