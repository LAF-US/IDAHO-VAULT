name: Branch & PR Cleanup

on:
  pull_request:
    types: [closed]
    branches:
      - main
  schedule:
    - cron: '0 9 * * 1'  # Every Monday at 9am UTC
    - cron: '0 13 * * *'  # Daily at 1pm UTC - stale PR sweep
  group: branch-cleanup
  cancel-in-progress: false

jobs:
  cleanup:
    # On PR close: only run for agent/bot branches (merged or unmerged)
    # On schedule/dispatch: run the full sweep
    if: >
      github.event_name == 'schedule' ||
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' && (
        startsWith(github.event.pull_request.head.ref, 'claude/') ||
        startsWith(github.event.pull_request.head.ref, 'codex/') ||
        startsWith(github.event.pull_request.head.ref, 'gemini/') ||
        startsWith(github.event.pull_request.head.ref, 'copilot/') ||
        startsWith(github.event.pull_request.head.ref, 'perplexity/') ||
        startsWith(github.event.pull_request.head.ref, 'grok/') ||
        startsWith(github.event.pull_request.head.ref, 'bot/') ||
        startsWith(github.event.pull_request.head.ref, 'serena/') ||
        startsWith(github.event.pull_request.head.ref, 'ingest-') ||
        startsWith(github.event.pull_request.head.ref, 'wayback-audit-') ||
        startsWith(github.event.pull_request.head.ref, 'topology-census-')
      ))
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4
        with:
          fetch-depth: 0

      - name: Prune stale agent branches
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          EVENT_NAME: ${{ github.event_name }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_MERGED: ${{ github.event.pull_request.merged }}
          PR_HEAD_REF: ${{ github.event.pull_request.head.ref }}
          DRY_RUN: ${{ inputs.dry_run || 'false' }}
          STALE_AGE_DAYS: 7
          STALE_BEHIND_COMMITS: 100
        run: |
          AGENT_BRANCH_PATTERNS="claude/ codex/ gemini/ copilot/ perplexity/ grok/ bot/ serena/ ingest- wayback-audit- topology-census-"
          DELETED=0
          KEPT=0
          DELETED_LIST=""

          delete_branch() {
            local REF="$1"
            local REASON="$2"
            if [ "$DRY_RUN" = "true" ]; then
              echo "  [DRY RUN] Would delete ($REASON): $REF"
            else
              echo "  Deleting ($REASON): $REF"
              git push origin --delete "$REF" 2>/dev/null || echo "  Failed to delete (may be protected or already removed): $REF"
            fi
            DELETED=$((DELETED + 1))
            DELETED_LIST="${DELETED_LIST}\n  - \`${REF}\` (${REASON})"
          }

          # On PR close: set an exact lifecycle state, then prune the branch.
          if [ "$EVENT_NAME" = "pull_request" ]; then
            BRANCH="$PR_HEAD_REF"
            if [ "$PR_MERGED" = "true" ]; then
              LIFECYCLE_STATE="merged"
            else
              LIFECYCLE_STATE="abandoned"
            fi

            if [ -n "$PR_NUMBER" ]; then
              python3 .github/scripts/pr_lifecycle.py ensure-labels
              python3 .github/scripts/pr_lifecycle.py set-state --pr-number "$PR_NUMBER" --state "$LIFECYCLE_STATE"
            fi

            echo "PR closed - lifecycle/$LIFECYCLE_STATE - pruning branch: $BRANCH"
            delete_branch "$BRANCH" "$LIFECYCLE_STATE"
            echo "Done."
            exit 0
          fi

