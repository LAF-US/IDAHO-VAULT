name: check-notebooks-paired

# The "middle" guardrail: every explicitly-paired Jupyter notebook must stay in sync with its
# Markdown twin. Mirrors the repo's other check-* workflows. Installs jupytext at the exact
# version uv.lock pins (the single source of truth), so the runner matches the version that
# wrote the committed twins and can't rewrite their stamp. Actions are pinned to full SHAs
# per repo policy.

on:
  pull_request:
    paths:
      - "**/*.ipynb"
      - "**/*.md"
      - "jupytext.toml"
      # Re-validate the guardrail when the guardrail itself changes, so edits to the
      # enforcement script or this workflow can't land without running the check they implement.
      - ".github/scripts/jupytext_sync_paired.py"
      - ".github/workflows/check-notebooks-paired.yml"
  workflow_dispatch:

permissions:
  contents: read

# Supersede, don't stack: a second push to the same branch cancels the still-running
# check for the previous commit, which nobody is waiting on. The final state of the
# branch is still checked in full, so no coverage is lost.
#
# `github.event_name` is in the group key on purpose: it keeps the push-event run and
# the pull_request-event run of the same branch in SEPARATE groups. Collapsing them
# would let a push run cancel a required PR check, and a cancelled required check
# blocks the PR.
#
# merge_group is never cancelled — cancelling a merge-queue check breaks the queue.
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: ${{ github.event_name != 'merge_group' }}

jobs:
  paired:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"
      - name: Install jupytext (pinned to uv.lock so CI matches the committed twins)
        run: |
          # The twins stamp the jupytext version that wrote them; a newer jupytext on the
          # runner would rewrite that stamp and fail the drift check. Install the EXACT
          # version uv.lock pins, not a floating range that resolves to whatever is newest.
          version=$(python -c "import tomllib; lock = tomllib.load(open('uv.lock', 'rb')); print(next(p['version'] for p in lock['package'] if p['name'] == 'jupytext'))")
          echo "jupytext $version (pinned by uv.lock)"
          python -m pip install --quiet "jupytext==$version"
      - name: Sync every paired notebook and fail on drift
        run: |
          # Sync ONLY notebooks that declare a pairing (explicit opt-in). A sync error on a
          # paired notebook fails the job — no more silent '|| true' that could pass green while
          # a paired notebook failed to sync. Unpaired or unparseable (corrupt) notebooks are
          # skipped, never synced. Then any drift against a committed twin fails the job.
          python .github/scripts/jupytext_sync_paired.py
          if ! git diff --quiet; then
            echo "::error::A notebook twin is out of sync. Run 'jupytext --sync <notebook>' and commit the twin."
            git --no-pager diff --stat
            exit 1
          fi
